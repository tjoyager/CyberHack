run = "cd backend && pip install -r requirements.txt && uvicorn app.main:app --host 0.0.0.0 --port 8080"
entrypoint = "backend/app/main.py"
modules = ["nodejs-20", "python-3.12", "web"]

[nix]
channel = "stable-25_05"
